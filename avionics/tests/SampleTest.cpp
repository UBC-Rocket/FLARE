#include "gtest/gtest.h"

/*
 * Sample Test Template
 * 
 * Add Tests: 
 *      For adding new tests, you will also need to link those tests in CMakeLists.txt.
 *
 * Run Tests:
 *      Build project in build folder, then run "make test"
 */

TEST(SampleTest, SampleTestTemplate)
{
    EXPECT_EQ(2, 2);
    ASSERT_EQ(3, 3);
    ASSERT_TRUE(true);
}